# Rapport — [TD3]

## Points clés

### 1. [Expliquez le modèle événement → état → rendu]

[Ce modèle sert à comprendre comment fonctionne une interaction sur un site web. Premièrement, il y a un événement, quelque chose que fait l’utilisateur. Ensuite, cet événement provoque un changement d’état (menu qui s'ouvre par exemple. Enfin, il y a le rendu, donc ce qui change visuellement à l’écran.]

### 2. [Expliquez en quoi classList.toggle est la passerelle entre JS et CSS]

[classList.toggle est la passerelle entre JavaScript et CSS parce qu’il permet de relier le comportement et l’apparence. Le JS détermine qu’un changement doit se produire. Pour le faire, il ajoute ou enlève une classe avec classList.toggle. Ensuite, c’est le CSS qui utilise cette classe pour modifier l’affichage.]
 
## Utilisation de l'IA

### Démarche

[ChatGPT]

### Apports et limites

[Corriger les fautes dans les réponses et mieux comprendre à quoi sert la commande classList.toggle.]
